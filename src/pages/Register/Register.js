import React from 'react';
import { FormLayout, RegisterForm } from 'components';

function Register() {
  return (
    <FormLayout>
      <RegisterForm />
    </FormLayout>
  );
}

export default Register;
